<h2>System Logs</h2>

<table>
    <thead>
        <tr>
            <th>EventCode</th>
            <th>SourceName</th>
            <th>Message</th>
        </tr>
    </thead>
    <tbody>
        {{#each this.model.logs as |log|}}
        <tr>
            <td>{{log.EventCode}}</td>
            <td>{{log.SourceName}}</td>
            <td>{{log.Message}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>
<br>

{{!-- Pagination controls --}}
<div>
    {{!-- Check if model and totalPages are defined --}}
    {{#if this.model}}
    {{#if this.model.totalPages}}

    <button type="button" {{on "click" this.previousPage}}>Previous</button>

    {{this.page}}

    <button type="button" {{on "click" this.nextPage}}>Next</button>

    {{/if}}
    {{/if}}
</div>